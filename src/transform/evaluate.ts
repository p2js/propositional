import * as AST from '../syntax/ast';
import { variable } from '../syntax/generate';
function evaluate(expression: AST.Expression, values: { [key: string]: number | boolean }): boolean {
    for (let variable in values) {

    }
    throw "";
}